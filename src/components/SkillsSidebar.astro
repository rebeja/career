---
interface Props {
  skills: string[];
}
const { skills } = Astro.props;
---

<div class="skills-sidebar">
  <p class="skills-sidebar__label">Skills &amp; Tools</p>
  <ul class="skills-sidebar__list">
    {skills.map((skill) => (
      <li>{skill}</li>
    ))}
  </ul>
</div>
